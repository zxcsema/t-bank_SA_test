# Открытые вопросы к заказчику

1. Какое значение `confidence` считается низким?
2. Одинаков ли порог `confidence` для `HOTEL`, `ACTIVITY` и `ROUTE`?
3. Какое максимальное время ответа всей системы допустимо для пользователя?
4. Сколько рекомендаций нужно возвращать по умолчанию?
5. Можно ли возвращать частичный результат, если найдено меньше объектов, чем указано в `limit`?
6. Какие действия пользователя считаются явным отклонением объекта?
7. Нужно ли объяснять пользователю причины рекомендации через `reasonCodes` в интерфейсе?
8. Какой срок хранения технических логов допустим по внутренним политикам?
9. Нужно ли учитывать состав поездки: один пользователь, семья, деловая поездка?
10. Должен ли fallback использовать разные веса для отелей, активностей и маршрутов?

